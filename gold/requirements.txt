# 기본 라이브러리 (플랫폼별 TensorFlow 선택)
tensorflow-macos>=2.16.1; platform_system=="Darwin" and platform_machine=="arm64"
tensorflow-metal>=1.1.0; platform_system=="Darwin" and platform_machine=="arm64"
tensorflow>=2.20.0; platform_system != 'Darwin' or platform_machine != 'arm64'

# 공통 과학 스택
pandas>=2.2.2
matplotlib>=3.8.4,<3.10
seaborn>=0.13.2

# NumPy/Sklearn 버전 호환성 조정
numpy>=1.26.4,<2.0; platform_system=="Darwin" and platform_machine=="arm64"
scikit-learn>=1.3.2,<1.4; platform_system=="Darwin" and platform_machine=="arm64"
numpy>=2.0.0; platform_system != 'Darwin' or platform_machine != 'arm64'
scikit-learn>=1.4.0; platform_system != 'Darwin' or platform_machine != 'arm64'
yfinance>=0.2.60
ta>=0.11.0

# 자동화 시스템
optuna>=4.5.0
mlflow>=3.3.0